# Permalinks
permalink:           pretty

# Setup
title:               pstopia
tagline:             Notes for Problem Solving Contest
baseurl:             https://ntopia.github.io/pstopia/

# Assets
#
# We specify the directory for Jekyll so we can use @imports.
sass:
  sass_dir:          _sass
  style:            :compressed

# Gems
gems:
  - jekyll-paginate
  - jekyll-gist
